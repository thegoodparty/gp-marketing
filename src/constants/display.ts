/**
 * Default display count for paginated lists.
 * Used when initialDisplayCount is not provided.
 */
export const DEFAULT_DISPLAY_COUNT = 24;

/**
 * Default year offset for calculating default year.
 * Used to calculate defaultYear as current year + offset.
 */
export const DEFAULT_YEAR_OFFSET = 1;
