import { Meta, Story, Canvas, Controls, Description } from '@storybook/addon-docs/blocks';
import * as ElectionsPositionContentBlockStories from './ElectionsPositionContentBlock.stories';

<Meta of={ElectionsPositionContentBlockStories} />

# Elections Position Content Block

<Description of={ElectionsPositionContentBlockStories.Filing} />

## Overview

The body of a position page below the hero. A side rail on the left ("On this page" links, an
"Explore more races" card and a share card) and a column on the right holding, in order: the badge
callout, the seat filter, the candidates list, the officeholders list, the branded CTA, the voter
readiness card, the About card and the How to run steps.

The block has one `state`, shared with the hero (`resolvePositionHeroState`), so the two can never
disagree about where the race is in its cycle. Filing and mid-election render the same body. Once
decided, the list heading becomes the results heading, winners get an ELECTED tag, the branded CTA
speaks about the winner and the "This election is over" banner appears above the steps.

## Hiding rules

Every data-fed section hides on its own when a page has nothing for it, and the "On this page"
links name only the sections that rendered:

- Candidates and officeholders: hidden when `undefined` (no data) and when empty.
- Badge callout: hidden unless a people list is showing.
- Seat filter: hidden unless the route built one with more than one option.
- About: hidden without a description or an attribute.
- How to run steps 1 and 2: hidden without eligibility or filing data. Step 3 is editorial and always shows.
- Explore card: hidden without a location href. Share card: hidden without a URL.

## Variants

<Canvas of={ElectionsPositionContentBlockStories.Filing} />

<Canvas of={ElectionsPositionContentBlockStories.MidElection} />

<Canvas of={ElectionsPositionContentBlockStories.DecidedPledgedWinner} />

<Canvas of={ElectionsPositionContentBlockStories.DecidedNoPledgedWinner} />

<Canvas of={ElectionsPositionContentBlockStories.DecidedMultipleWinners} />

<Canvas of={ElectionsPositionContentBlockStories.NoCandidateData} />

<Canvas of={ElectionsPositionContentBlockStories.OfficeholdersOnly} />

<Canvas of={ElectionsPositionContentBlockStories.WithSeatFilter} />

<Canvas of={ElectionsPositionContentBlockStories.SparseRaceData} />

<Canvas of={ElectionsPositionContentBlockStories.Midnight} />

## Props

<Controls of={ElectionsPositionContentBlockStories.Filing} />

## Where the data comes from

On the position pages the section wrapper (`ElectionsPositionContentBlockSection`) receives the
route's override from `buildPositionContentOverride`: the race dates, the same candidate rows the
hero shows, the current officeholders (`loadPositionOfficeholders`), the race facts for the About
card and the filing details for the steps. Every sentence is a Sanity field with a default in
`POSITION_CONTENT_DEFAULTS`, resolved with the page's tokens (`[office name]`, `[Position Name]`,
`[County or City]`).
