import { Meta, Story, Canvas, Controls, Description } from '@storybook/addon-docs/blocks';
import * as ProfileContentBlockStories from './ProfileContentBlock.stories';

<Meta of={ProfileContentBlockStories} />

# Profile Content Block

<Description of={ProfileContentBlockStories.Default} />

## Overview

The ProfileContentBlock component displays a two-column layout with an optional sticky sidebar (ElectionsSidebar) on the left and a vertical stack of ProfileContentCard components on the right. It's designed for candidate profile pages to show detailed information about the candidate.

## Usage

Use this component when:
- Displaying candidate profile content sections
- Showing multiple content cards (About Me, Why Running, Top Issues, etc.)
- Providing contact links (Website, Facebook, Email) in a sidebar
- Displaying office information (About Office, Term Length, Election Date) in a sidebar
- Creating a sticky sidebar for navigation or key information

## Variants

<Canvas of={ProfileContentBlockStories.Default} />

<Canvas of={ProfileContentBlockStories.WithoutSidebar} />

<Canvas of={ProfileContentBlockStories.MidnightBackground} />

<Canvas of={ProfileContentBlockStories.MultipleCards} />

<Canvas of={ProfileContentBlockStories.SidebarOnly} />

<Canvas of={ProfileContentBlockStories.LongUrls} />

<Canvas of={ProfileContentBlockStories.TabletStacked} />

## Props

<Controls of={ProfileContentBlockStories.Default} />

### Prop Details

#### `contentCards` (required)

Array of ProfileContentCard objects to display in the main content area.

**Type:** `ProfileContentCardProps[]`

Each card can have:
- `cardType` (optional) - `'about-me' | 'why-running' | 'top-issues'`
- `heading` (optional) - Card heading text
- `content` (optional) - Card content (ReactNode, supports rich text)

#### `sidebar` (optional)

ElectionsSidebar configuration. When provided, displays a sticky sidebar on the left.

**Type:** `ElectionsSidebarProps`

Sidebar can include:
- `links` - Array of link objects with label, optional icon, and href (e.g., Website, Facebook, Email)
- `aboutOffice` - Text describing the office position
- `termLength` - Text describing the term length
- `electionDate` - Text showing the election date
- `cta` - Optional CTA button (e.g., "Run for Office")

#### `backgroundColor` (optional)

Background color variant for the block.

**Type:** `'cream' | 'midnight'`

**Default:** `'cream'`

#### `className` (optional)

Additional CSS classes to apply to the component.

**Type:** `string`

## Layout

The component uses a responsive grid layout:
- **Mobile / tablet (below `lg`)**: Single column inside a centered `max-w-3xl` content well; sidebar and content cards share the same width and left edge
- **Desktop** (`lg:`): Two-column grid (well constraints removed)
  - Sidebar: up to `400px` wide (`minmax(280px, 400px)`), sticky while scrolling
  - Content: remaining width (`minmax(0, 1fr)`) with `min-w-0` so long copy wraps
  - Gap: `gap-8` below `lg`, `gap-10` at `lg`, `gap-12` at `xl`
- **Sidebar-only profiles**: Centered `max-w-3xl` well — sidebar fills the full well width; no sticky positioning or 400px cap (those apply only when content cards are present)

## Sticky Sidebar

When the sidebar is present on desktop (`lg:`) **and** content cards are rendered (two-column layout):
- Uses `lg:sticky lg:top-4 self-start` positioning
- Sidebar is capped at `400px` wide within the grid
- Stays visible while scrolling through content cards
- Below `lg`, the sidebar is not sticky (avoids content scrolling under the sidebar on tablet/mobile)

When the sidebar is the only content (no content cards), it spans the full `max-w-3xl` well and is not sticky.
- Provides quick access to contact links, office information, and CTA
- Displays links with icons (Website, Facebook, Email, etc.)
- Shows office details (About Office, Term Length, Election Date) with separators

## Content Cards

Content cards are displayed in a vertical stack:
- Gap: `gap-8` between cards
- Each card uses ProfileContentCard component
- Cards support rich text content via `isValidRichText` helper

## Accessibility

- Uses semantic `<article>` element
- Sidebar uses semantic `<aside>` element
- Content cards use semantic `<article>` elements
- Proper heading hierarchy maintained through ProfileContentCard headings

## Design Reference

[View in Figma](https://www.figma.com/design/dmMrTWyBirANhArKs5mTmr/GoodParty-Design-System----shadcn-ui?node-id=23711-25268&t=6DQyxTAl7Ola6AF3-0)

## Related Components

- `ProfileContentCard` - Individual content card component
- `ElectionsSidebar` - Sidebar component with filing period info
- `ProfileHero` - Hero section that typically precedes this component
