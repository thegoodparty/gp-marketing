export enum Alignment {
  LEFT = 'left',
  RIGHT = 'right',
  CENTER = 'center',
}

export enum IconPosition {
  LEFT = 'left',
  RIGHT = 'right',
}

export enum LinkTarget {
  BLANK = '_blank',
  SELF = '_self',
}
